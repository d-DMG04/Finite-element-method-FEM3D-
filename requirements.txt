# Зависимости управляющего слоя fem_heat3d.
# ============================================================================

# --- Обязательные ----------------------------------------------------------
numpy>=1.20            # массивы, ctypes-обмен с ядром
matplotlib>=3.4        # графики, резервный 3D-вид

# --- GUI -------------------------------------------------------------------
PyQt5>=5.15            # графический интерфейс

# --- Лучшая 3D-визуализация (рекомендуется) -------------------------------
pyvista>=0.40          # объёмный рендер, изоповерхности, picking
pyvistaqt>=0.11        # интеграция PyVista с Qt
vtk>=9.0               # подкладка PyVista

# --- Импорт и экспорт сеток -----------------------------------------------
meshio>=5.0            # чтение/запись MSH, VTU, VTK, и др.
gmsh>=4.10             # импорт STL/STEP с автоматической тетраэдризацией
                       #   (опционально — без него STL/STEP недоступны,
                       #    но MSH/VTU работают)

# --- Опционально для разработчика ------------------------------------------
# pytest>=7.0          # альтернативный раннер тестов
# scipy>=1.7           # для ускоренного KDTree в верификации T3
